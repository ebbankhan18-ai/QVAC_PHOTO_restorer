# Project Setup 
 
## SDK Version 
- Java SDK 21 (or your specific version) 
 
## Install & Run Steps 
1. Open terminal 
2. Run `npm install` (or your command) 
3. Run `npm start`
add upload UI and Express server for the restorer 
on-device photo restoration via QVAC SDK 
scaffold QVAC photo restorer project 
